stages:
  - deploy

deploy_smart_bulb_bot:
  stage: deploy
  script:
    - git pull origin master
    - docker compose build smart_bulb.bot
    - docker compose up --force-recreate --no-deps smart_bulb.bot
  only:
    refs:
      - master
